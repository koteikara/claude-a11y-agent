[pytest]
testpaths = tests
python_files = test_*.py
addopts = -q --strict-markers
markers =
    llm: 実LLMを呼ぶテスト（既定スキップ。RUN_LLM_TESTS=1 で有効化）
    drift: ai↔goldの情報提供ドリフト比較
    e2e: 実エージェント本体を接続するE2E回帰
