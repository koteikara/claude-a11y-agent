# Google auth uses Application Default Credentials or GOOGLE_APPLICATION_CREDENTIALS.
GOOGLE_APPLICATION_CREDENTIALS=/secrets/service-account.json
GOOGLE_SHEET_ID=your-google-sheet-id

# The backend reads these folder IDs from the Config sheet, but keeping them
# documented here helps operators validate Cloud Run/Secret Manager settings.
DRIVE_INPUT_FOLDER_ID=your-input-folder-id
DRIVE_OUTPUT_AI_FOLDER_ID=your-ai-folder-id
DRIVE_OUTPUT_GOLD_FOLDER_ID=your-gold-folder-id

# Prefer Cloud Run + IAP. For MVP/local private deployments only:
BASIC_AUTH_USERNAME=admin
BASIC_AUTH_PASSWORD=change-me
PORT=8080
